import React from "react";

const App = () => {
  return (
    <div>
      <p>This text is in the App file in the source (src) folder.</p>
      <p>This webpack.config.js file is set up so that HTML, CSS, Images, and
        routing should work without anything else needing to be added. You may
        need to change App from a const to a function if using react-router-dom
        on this page.
      </p>
    </div>
  );
};

export default App;
